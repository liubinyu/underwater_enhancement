# A40 server environment for AquaAlign-VLM QLoRA, SFT, and DPO.
#
# Install a CUDA-enabled PyTorch build BEFORE this file, using the command
# generated by https://pytorch.org/get-started/locally/ for the server driver.
# The bounds below prevent pip from silently accepting an old CPU-only build.
-r requirements-base.txt

opencv-python-headless>=4.8,<5
torch>=2.8,<2.12
torchvision>=0.23
ms-swift>=4.2,<5
transformers>=4.57.6,<5
modelscope>=1.23
datasets>=3.0,<4.8.5
peft>=0.11,<0.20
trl>=0.15,<1.0
accelerate>=1.0
bitsandbytes>=0.49,<0.50
qwen-vl-utils>=0.0.14
safetensors>=0.4
gradio>=5.32,<6

# Optional after the base smoke test succeeds:
# flash-attn is intentionally not pinned here because its wheel/source build
# must match the installed CUDA and PyTorch versions.
